---
/** Wraps write-up content that only renders in Plain English mode.
 *  Pair with <EngOnly>. Visibility is pure CSS (global.css), so both
 *  variants ship in the HTML and the header toggle flips instantly. */
---
<div class="only-plain"><slot /></div>
